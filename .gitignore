# ============================================
# DEPENDENCIES
# ============================================
# Ignore all node_modules folders (frontend & backend)
# These are installed via npm/yarn and shouldn't be in version control
node_modules/

# Lock files can be included or excluded based on team preference
# Uncomment the line below if you want to ignore lock files
# package-lock.json

# ============================================
# LOGS
# ============================================
# Ignore all log files generated during development/runtime
# These are temporary and contain debugging information
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# ============================================
# BUILD OUTPUTS
# ============================================
# Ignore compiled/bundled code (generated by Vite, Webpack, etc.)
# These are created from source code and shouldn't be committed
dist/           # Standard build output folder
dist-ssr/       # Server-side rendering build output (Vite)
build/          # Alternative build folder name
*.local         # Local build variants

# ============================================
# ENVIRONMENT VARIABLES
# ============================================
# CRITICAL: Never commit .env files - they contain secrets!
# (API keys, database passwords, JWT secrets, etc.)
.env                    # Root level environment file
.env.local              # Local overrides
.env.*.local            # Environment-specific local files
planner-backend/.env    # Backend-specific environment variables

# ============================================
# EDITOR & IDE FILES
# ============================================
# Ignore editor-specific settings (these vary per developer)
.vscode/*                      # VS Code settings folder
!.vscode/extensions.json       # EXCEPT recommended extensions (keep this)
.idea                          # JetBrains IDEs (WebStorm, IntelliJ)
*.suo                          # Visual Studio user options
*.ntvs*                        # Node Tools for Visual Studio
*.njsproj                      # Node.js project file
*.sln                          # Visual Studio solution file
*.sw?                          # Vim swap files

# ============================================
# OPERATING SYSTEM FILES
# ============================================
# OS-generated files that aren't part of the project
.DS_Store       # macOS folder metadata
Thumbs.db       # Windows thumbnail cache

# ============================================
# TESTING
# ============================================
# Ignore test coverage reports and test output
coverage/       # Code coverage reports
.nyc_output/    # NYC (Istanbul) coverage tool output

# ============================================
# TEMPORARY FILES
# ============================================
# Ignore temporary files created during development
*.tmp           # Temporary files
*.temp          # Temporary files (alternative extension)
.cache/         # Cache directories
